[Unit]
Description=WebGWAS Backend Service
After=network.target

[Service]
User=root
WorkingDirectory=/root/
Environment="RUST_LOG=info"
ExecStart=/root/webgwas_backend
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
